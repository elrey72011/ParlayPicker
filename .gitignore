# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.test-deps/
.pytest_cache/
.pytest-tmp/
.claude/.venv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Streamlit
old_streamlit/

# Historical data cache
historical_cache/
*.json
*.pkl
*.pickle

# Recap-fitted probability calibration (production input for parlay generation;
# refit with scripts/fit_calibration.py after grading each slate)
!data/calibration/effective_prob_calibration.json
# Realized bucket win rates (production input for the empirical tier overlay;
# refit with scripts/fit_bucket_stats.py after grading each slate)
!data/calibration/bucket_stats.json

# API Keys (IMPORTANT!)
.env
*.key
config.ini
secrets.toml

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
old_streamlit/secrets.toml
# Runtime copy of uploaded/generated prop history. The cumulative deploy-time
# baseline remains tracked at data/prop_results/prop_results_log.csv.
data/prop_results/prop_results_runtime.csv
# Runtime copy of cumulative candidate-ranking history.
data/candidate_results/candidate_results_runtime.csv
# Streamlit backups
streamlit_app.py.bak*
*.bak


# Strict closing sportsbook normalization — 2026-09-15

HEAD BEFORE: `110926ae11e0a391232f3f5f5081385c3afae9e0` (main, merged PR #2280).
HEAD AFTER: the commit containing this report on `codex/strict-closing-book-normalization`; exact hash is provided in the PR handoff.

## Reproduction

P1 reproduced through a real snapshot persisted by `capture_run`, reloaded by `load_snapshots`, and processed by `capture_live` with an injected live-provider response.

- Saved quote_bookmaker: DraftKings.
- Fresh raw quote book: draftkings.
- Provider namespace/event: odds_api / provider:123, identical on both sides.
- Before: verified=0, unavailable=1; reason=missing_or_ambiguous_exact_quote; stored observation count=0.
- Pre-fix regression: 1 failed, 9 deselected, 0 warnings, 0.93s.

The discovery filter compared raw strings; `record_close` independently made the same representation-sensitive comparison. The explicit verified-quotes CLI had an equivalent filter.

## Implementation

All three boundaries reuse `app_core.public_quote_policy.canonical_book_label`, without modifying that helper or its mappings.

- `capture_live`: canonicalizes both book identities for comparison and constructs the close with a canonical sportsbook.
- `record_close`: canonicalizes the sportsbook on a copy of the incoming quote and canonicalizes the candidate identity for comparison. Different books still fail. The saved candidate and caller's quote are not rewritten.
- `scripts/capture_closing_lines.py --verified-quotes`: uses the same comparison; `record_close` performs canonical persistence. `--prospective` already delegates to `capture_live`. Command syntax and older CLV export behavior are unchanged.
- Raw provider payload mutation: NO.

No event, namespace, market, timing, ambiguity, numeric-validity or integrity condition was relaxed. No CLV formula changed.

## End-to-end strict close

PASS: verified=1, unavailable=0; exactly one observation stored in the focused run.

| Persisted field | Value |
| --- | --- |
| candidate_id | close-candidate |
| snapshot_id | 826b90227bc543d2b9351440c794fb72 |
| quote.sportsbook | DraftKings |
| quote.provider_namespace | odds_api |
| quote.provider_event_id | provider:123 |
| quote.quote_recorded_at | 2026-09-14T15:00:00+00:00 |
| quote_verified | true |

The snapshot ID is generated by the isolated fixture; tests assert exact equality with the persisted source. Raw fetched DataFrames and saved snapshots are unchanged.

## Acceptance evidence

| Requirements | Result and coverage |
| --- | --- |
| CLOSE-01 through CLOSE-05 | PASS: persisted candidate matrix covers DraftKings, FanDuel, BetMGM, Novig, novig_us and already canonical input. |
| CLOSE-06 through CLOSE-10; SAFE-03 through SAFE-06 | PASS: wrong/unknown book, provider namespace, event ID, market and matchup remain non-matching. |
| CLOSE-11; SAFE-07 | PASS: stale, post-start and malformed quote times produce invalid_or_stale_close. |
| CLOSE-12; SAFE-01/02 | PASS: missing quotes and two matching closes remain missing_or_ambiguous_exact_quote; no observations saved. |
| RECORD-01 | PASS: raw/canonical direct inputs persist canonically; equivalent representation returns the same digest; raw historical candidate identity is accepted without rewriting it. |
| RECORD-02/03 | PASS: wrong sportsbook/game/sport/market/event/namespace still raises. |
| E2E-01 | PASS: real persistence, reload, capture and observation read; no snapshot-loader mock. |
| CLV-01 | PASS: unchanged-line -110 to -120 preserves price_clv and beat_close; -2.5 to -3 preserves line_clv=0.5, price_clv=None and beat_close=true. |
| EVID-01 | PASS: duplicate capture is idempotent; new observation appends without changing the prior record; UPDATE/DELETE are rejected; corrupt digest is rejected on read. |
| SAFE-08/09/10 | PASS: no policy activation, bankroll/exposure changes or sportsbook execution. |
| SCOPE-01 | PASS: only closing boundary, its CLI caller, regressions and this report changed. |

## Validation

| Check | Result |
| --- | --- |
| Focused (four activation/closing/CLV modules) | 79 passed, 0 failed, 0 warnings; 10.08s |
| Full pytest -q | 2309 passed, 0 failed, 9828 warnings; 109.18s |
| Exact CI production-safety command | 531 passed, 0 failed, 1430 warnings; 32.56s |
| Exact CI production compile | PASS |
| git diff --check | PASS |

TEST-01 through TEST-05: PASS. Tests ran locally on Windows with the configured Python, test dependencies and Node runtime. Hosted CI will run after push. Existing warnings were not addressed in this narrowly scoped patch.

The suites use isolated local test databases and injected provider responses. No real policy was activated and no historical evidence was modified.

## Files changed

- app_core/activation_closing.py
- scripts/capture_closing_lines.py
- tests/test_activation_reverification.py
- docs/audits/2026-09-15-strict-closing-book-normalization.md

Unrelated changes in this patch: NONE. Pre-existing untracked Medium article files remain untouched and excluded.

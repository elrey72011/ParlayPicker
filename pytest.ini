[pytest]
# Match the explicit tests/ suite used by GitHub CI. Root-level diagnostics
# remain manually invokable and are not part of default test discovery.
testpaths = tests

markers =
    activation_acceptance: hermetic funding and fail-closed activation contracts
